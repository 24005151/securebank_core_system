"""
SecureBank — routes sub-package.

I contain three route modules:
    auth.py   — login, logout, CSRF token, /me
    pages.py  — HTML page views (/, /login)
    api.py    — all data API endpoints
"""
